/** @format */

import { makeStyles } from '@mui/styles';

const useStyles = makeStyles((theme) => ({}));

const Feed = () => {
  const classes = useStyles();
  return <div>feed</div>;
};
export default Feed;
